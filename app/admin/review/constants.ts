/** Cap in-memory scan for multi-brand (most recent listings in the review window). */
export const MULTI_BRAND_CANDIDATE_CAP = 5000;

/** Same recent window as the review queue SQL filters. */
export const REVIEW_RECENT_DAYS = 14;
